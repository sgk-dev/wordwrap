[Unit]
Description=SGK WordWrap - automatic keyboard layout switcher
Documentation=https://github.com/sgk-dev/wordwrap
After=graphical-session.target
PartOf=graphical-session.target
StartLimitIntervalSec=60
StartLimitBurst=5

[Service]
Type=simple
ExecStart=%h/.local/bin/sgk-wordwrap
Restart=on-failure
RestartSec=3s
# Session environment. install.sh also runs `systemctl --user import-environment`
# so WAYLAND_DISPLAY / DBUS_SESSION_BUS_ADDRESS are populated for the unit.
Environment=PYTHONUNBUFFERED=1
Environment=DISPLAY=:0
PassEnvironment=WAYLAND_DISPLAY XDG_CURRENT_DESKTOP XDG_SESSION_TYPE DBUS_SESSION_BUS_ADDRESS

# Resource limits - keep the daemon lightweight
MemoryMax=128M
CPUQuota=10%

[Install]
WantedBy=graphical-session.target
